# ── Core data / geospatial stack ────────────────────────────────

pandas>=2.1
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
pyogrio

# ── OSMnx / Overpass backend ────────────────────────────────────

osmnx>=1.9

# ── PostGIS backend ─────────────────────────────────────────────

psycopg2-binary>=2.9

# ── System dependencies ─────────────────────────────────────────

# Osmium backend:

# Requires osmium-tool

# Ubuntu/Debian: sudo apt install osmium-tool

# macOS:         brew install osmium-tool

# PostGIS backend:

# Requires PostgreSQL, PostGIS, osm2pgsql, and PostgreSQL CLI tools

# (createdb, dropdb, psql)
